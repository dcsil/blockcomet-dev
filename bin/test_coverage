#!/usr/bin/env bash
pwd 
DIR="$(pwd)"
echo ${DIR}

echo "Running frontend tests"
cd $DIR/app/client/
yarn test --coverage

echo "Running backend tests"
cd ../server/
poetry run coverage run -m pytest
poetry run coverage xml -o xml_coverage.xml

#!/usr/bin/env bash
pwd 
DIR="$(pwd)"
echo ${DIR}

echo "Running frontend tests"
cd $DIR/app/client/
yarn test --coverage

echo "Running backend tests"
cd ../server/
poetry run coverage lcov -o lcov_coverage pytest
